<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; }
        .item-list { list-style: none; padding: 0; }
        .folder, .video-item { cursor: pointer; padding: 5px 10px; border-radius: 5px; display: flex; align-items: center; gap: 10px; }
        .folder { font-weight: bold; color: #007bff; }
        .folder::before { content: "📁"; }
        .thumbnail { width: 120px; height: 70px; object-fit: cover; border-radius: 5px; }
        .nested { display: none; margin-left: 20px; }
        .expanded > .nested { display: block; border-left: 1px lightgray solid; }
    </style>
</head>
<body>
<h1>Library</h1>
<ul id="library-container" class="item-list"></ul>

<script>
    async function fetchLibrary() {
        try {
            const response = await fetch('/library/list');
            const data = await response.json();
            renderLibrary(data, document.getElementById('library-container'));
        } catch (error) {
            console.error('Error loading library:', error);
        }
    }

    function renderLibrary(items, container) {
        items.forEach(item => {
            const li = document.createElement('li');

            if (item.type === 'folder') {
                li.className = 'folder';
                li.textContent = item.name;
                const nestedUl = document.createElement('ul');
                nestedUl.className = 'nested';
                li.appendChild(nestedUl);

                li.addEventListener('click', (e) => {
                    e.stopPropagation();
                    li.classList.toggle('expanded');
                    if (!nestedUl.hasChildNodes()) {
                        renderLibrary(item.children, nestedUl);
                    }
                });
            } else if (item.type === 'video') {
                li.className = 'video-item';
                li.innerHTML = `
                        <a href="${item.path}">
                            <img class="thumbnail" src="${item.thumbnail}" alt="Thumbnail">
                            ${item.name}
                        </a>
                    `;
            }

            container.appendChild(li);
        });
    }

    fetchLibrary();
</script>
</body>
</html>
